import React from 'react';
import ReactDOM from 'react-dom';
import {MemodFuncComponent} from './App';

ReactDOM.render(
    <React.StrictMode>
        <MemodFuncComponent/>
    </React.StrictMode>,
    document.getElementById('root')
);